// C++
#include <iostream>
#include "OneXAPI.hpp"

int main(){
    OneXAPI::Binance::Spot client;

    std::string request = R"(
        {
            "market":[
                {
                    "baseCurrency": "BTC",
                    "quoteCurrency": "USDT"
                },
                {
                    "baseCurrency": "ETH",
                    "quoteCurrency": "USDT"
                }
            ]
        }
    )";
    std::cout << client.subscribeTicker(request) << std::endl;
    
    return 0;
}